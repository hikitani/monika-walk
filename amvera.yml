meta:
  environment: golang
  toolchain:
    name: go
    version: "1.22"
build:
  skip: false
  dockerfile: Dockerfile
run:
  image: golang:1.22
  persistenceMount: /data
  containerPort: "8080"
  servicePort: "80"
